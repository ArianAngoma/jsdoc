/**
 * Lista de Edades de usuarios
 * @type {Array}
 * */
const age = [10, 11, 12, 13, 14, 15];

/**
 * Lista de Puntajes de usuarios
 * @type {Array<Number>}
 * */
const score = [100, 200, 300, 400, 500];

/**
 * Arreglo de Números y Strings
 * @type {Array<Number | string>}
 * */
const numberAndString = [10, "Arian", 20, "Angoma", 30, "Vilchez"];